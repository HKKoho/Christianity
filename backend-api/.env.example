# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration
# MongoDB Atlas connection string
# Format: mongodb+srv://username:password@cluster.mongodb.net/dbname
# Important: URL encode special characters in password (@ becomes %40)
DATABASE_URL=mongodb+srv://username:password@cluster.mongodb.net/christianityPlatform?retryWrites=true&w=majority

# JWT Authentication
# Use a strong random string (minimum 32 characters)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long
JWT_EXPIRES_IN=7d

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ORIGIN=http://localhost:3000,http://localhost:5173

# Frontend URL
# Main platform URL for redirects and CORS
FRONTEND_URL=http://localhost:5173

# Optional: Prisma Configuration
# If using Prisma instead of native MongoDB driver
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/christianityPlatform
